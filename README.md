# LoveDate (Expo)

## Install
```bash
npm install
```

## Run (Expo Go)
```bash
npx expo start
```
Scan the QR in Expo Go.

## iOS build (TestFlight/App Store)
```bash
npm i -g eas-cli
eas login
eas build:configure
eas build -p ios --profile production
```
